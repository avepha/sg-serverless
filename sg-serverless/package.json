{
  "name": "serverless-es6",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run:local:save_logger": "serverless invoke local --function SensorLoggerSave --logs",
    "run:server:save_logger": "serverless invoke --function SensorLoggerSave --logs",
    "deploy": "serverless deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-webpack": "^5.3.0",
    "webpack": "^4.30.0"
  },
  "dependencies": {
    "@aws/dynamodb-data-mapper": "^0.7.3",
    "aws-sdk": "^2.446.0",
    "lodash": "^4.17.11",
    "moment": "^2.24.0"
  }
}
